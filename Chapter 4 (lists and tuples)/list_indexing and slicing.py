#Indexing starts from 0 in lists too
#Lists are mutable, we can add stuff in it, remove stuff from it etc.

#List slicing is the same as string slicing.

friends = ["abc", 19.3, 13, False]
print(friends[1:3]) #Prints the 1st and the 2nd index, doesnt print the last value.
