n :int = 5 #:int tells python that the value is an integer.
name :str = "Izumi"

def sum(a :int, b :int): #We can define that the parameter will take integer as an argument. This makes the code easier to use.
    return a + b

